// single-line comment

/* block comment
   spanning multiple lines */

program {
  extern void foo(integer x, bool y);

  integer main() {
    integer a = -42;
    bool b = true;
    bool c = false;

    my_id123 = 12345;

    a = a + 5 * (3 - 1) / 2 % 7;

    if a < 100 && !b || c then {
      return a;
    } else {
      while a > 0 {
        a = a - 1;
      }
      if a == 10 then { return a; } else { return a; }
      return a;
    }
  }
}