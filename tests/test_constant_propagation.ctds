// Test para optimizacion de propagacion de constantes
program {
    void main() {
        integer x = 5;
        integer y = 10;
        integer z = 0;
        integer w = 0;
        integer a = 10;
        integer b = 5;
        
        z = x + y;      // Deberia plegarse a z = 15
        w = z * 2;      // Deberia plegarse a w = 30
        a = 3 + 7;      // Deberia plegarse a a = 10
        b = a - 5;      // Deberia plegarse a b = 5
    }
}
