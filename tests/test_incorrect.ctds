program {
    void main() {
        integer y = 0          // falta ';'
        if (y == 1) {          // falta 'then' después del paréntesis
            print_int(y);
        } else {
            print_int(inc(y));
        }
    }
}